
services:
  zomato:
    image: manikantarouthu/vinay:zomato
    ports:
      - "3000:3000"
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
    networks:
      - zomato-net
networks:
  zomato-net:
    driver: overlay

